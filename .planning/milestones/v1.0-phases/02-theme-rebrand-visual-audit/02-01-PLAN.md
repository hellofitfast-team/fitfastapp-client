---
phase: 02-theme-rebrand-visual-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/(dashboard)/meal-plan/page.tsx
  - src/app/[locale]/(dashboard)/workout-plan/page.tsx
  - src/app/[locale]/(dashboard)/page.tsx
autonomous: true

must_haves:
  truths:
    - "No inline style={{}} attributes with hardcoded hex values in dashboard pages"
    - "No useState hover anti-patterns in meal-plan or workout-plan pages"
    - "All hover/focus states use Tailwind utility classes, not JavaScript state"
  artifacts:
    - path: "src/app/[locale]/(dashboard)/meal-plan/page.tsx"
      provides: "Meal plan page with Tailwind hover classes, no inline styles"
      contains: "hover:bg-primary"
    - path: "src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      provides: "Workout plan page with Tailwind hover classes, no inline styles"
      contains: "hover:bg-primary"
    - path: "src/app/[locale]/(dashboard)/page.tsx"
      provides: "Dashboard home with Tailwind classes, no inline styles"
      contains: "bg-black text-cream"
  key_links:
    - from: "src/app/[locale]/(dashboard)/meal-plan/page.tsx"
      to: "src/app/globals.css"
      via: "Tailwind semantic color classes"
      pattern: "bg-primary|text-primary|hover:bg-primary"
    - from: "src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      to: "src/app/globals.css"
      via: "Tailwind semantic color classes"
      pattern: "bg-primary|text-primary|hover:bg-primary"
---

<objective>
Eliminate inline style anti-patterns and hardcoded hex values from all three dashboard page files (home, meal-plan, workout-plan).

Purpose: Phase 1 replaced most hardcoded hex values but left behind inline `style={{}}` attributes and `useState` hover patterns. These are anti-patterns (per MEMORY.md: "Use Tailwind hover: classes, not useState + onMouseEnter/onMouseLeave -- avoids unnecessary re-renders"). This plan converts them all to proper Tailwind utility classes.

Output: Three dashboard pages with zero inline style attributes containing hex values, zero useState hover state variables, all using Tailwind hover:/focus: classes.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-rebrand-core-color-swap/01-01-SUMMARY.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix meal-plan and workout-plan hover anti-patterns and inline styles</name>
  <files>
    src/app/[locale]/(dashboard)/meal-plan/page.tsx
    src/app/[locale]/(dashboard)/workout-plan/page.tsx
  </files>
  <action>
In both meal-plan/page.tsx and workout-plan/page.tsx:

1. **Remove all useState hover variables:**
   - `hoveredNewPlanBtn`, `hoveredMealButtons`, `hoveredGenerateBtn`, `hoveredStartBtn`
   - Remove corresponding `setHovered*` calls

2. **Remove all onMouseEnter/onMouseLeave handlers** that toggle hover state.

3. **Convert all inline style={{}} with hover logic to Tailwind classes:**

   For meal-plan/page.tsx:
   - Line ~146: `backgroundColor: hoveredNewPlanBtn ? '#4169E1' : '#FFFEF5'` -> Replace the entire element's style attribute with Tailwind classes: `bg-cream hover:bg-primary hover:text-cream` (or appropriate text color class)
   - Line ~249: `backgroundColor: hoveredMealButtons[index] ? '#4169E1' : '#FFFEF5'` -> Same pattern: `bg-cream hover:bg-primary hover:text-cream`

   For workout-plan/page.tsx:
   - Line ~103-104: `backgroundColor: hoveredGenerateBtn ? '#4169E1' : '#000000'`, `color: hoveredGenerateBtn ? '#FFFFFF' : '#FFFEF5'` -> `bg-black text-cream hover:bg-primary hover:text-white`
   - Line ~153: `backgroundColor: hoveredNewPlanBtn ? '#4169E1' : '#FFFEF5'` -> `bg-cream hover:bg-primary hover:text-cream`
   - Lines ~244-245: `backgroundColor: hoveredStartBtn ? '#4169E1' : '#000000'`, `color: hoveredStartBtn ? '#FFFFFF' : '#FFFEF5'` -> `bg-black text-cream hover:bg-primary hover:text-white`

4. **Ensure the `style` attribute is completely removed** from each converted element (not just emptied).

5. **Keep all non-hover inline styles** (if any exist for dynamic values like calculated widths). Only remove style attributes that contain hardcoded hex color values.

Important: Preserve ALL other functionality (onClick handlers, conditional rendering, className logic, translations). Only change the color/hover mechanism.
  </action>
  <verify>
Run: `grep -n "hoveredGenerateBtn\|hoveredNewPlanBtn\|hoveredStartBtn\|hoveredMealButtons\|onMouseEnter\|onMouseLeave" src/app/\[locale\]/\(dashboard\)/meal-plan/page.tsx src/app/\[locale\]/\(dashboard\)/workout-plan/page.tsx` -- should return zero matches.

Run: `grep -c "style=" src/app/\[locale\]/\(dashboard\)/meal-plan/page.tsx src/app/\[locale\]/\(dashboard\)/workout-plan/page.tsx` -- should return 0 for each file (or only non-color dynamic styles).

Run: `pnpm tsc --noEmit` -- passes with no errors.
  </verify>
  <done>
Both meal-plan and workout-plan pages have zero useState hover variables, zero onMouseEnter/onMouseLeave handlers, and all color styling uses Tailwind utility classes (hover:bg-primary, bg-cream, bg-black, text-cream, etc.). TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert dashboard home page inline styles to Tailwind classes</name>
  <files>
    src/app/[locale]/(dashboard)/page.tsx
  </files>
  <action>
The dashboard home page (page.tsx) has many inline style={{}} attributes with hardcoded hex values. Convert ALL to Tailwind utility classes.

Specific conversions needed:

1. **Marquee banner** (~line 117):
   `style={{ backgroundColor: "#000000", color: "#FFFEF5" }}` -> `className="... bg-black text-cream"`

2. **Workout card done state** (~lines 238-291, multiple elements):
   These use conditional inline styles like `style={dashboardData.todaysWorkout.done ? { backgroundColor: "#000000", color: "#FFFEF5" } : undefined}`.
   Convert to conditional Tailwind classes using template literals or cn() utility:
   - `backgroundColor: "#000000"` -> class `bg-black`
   - `color: "#FFFEF5"` -> class `text-cream`
   - `color: "#a3a3a3"` -> class `text-neutral-400`
   - `color: "#737373"` -> class `text-neutral-500`
   - `borderColor: "#FFFEF5"` -> class `border-cream`
   - `borderColor: "#000000"` -> class `border-black`

   Pattern for conditional styles:
   ```tsx
   className={cn("base-classes", dashboardData.todaysWorkout.done && "bg-black text-cream")}
   ```
   Make sure to import `cn` from `@/lib/utils` if not already imported.

3. **Footer section** (~line 355):
   `style={{ backgroundColor: "#000000", color: "#FFFEF5" }}` -> `className="... bg-black text-cream"`

4. **Footer links** (~lines 361-363):
   `style={{ color: "#FFFEF5" }}` -> `className="... text-cream"`

After all conversions, the page.tsx file should have ZERO `style={{` attributes containing hex color values. The only acceptable remaining inline styles would be for truly dynamic values (e.g., calculated percentages, runtime-determined widths).
  </action>
  <verify>
Run: `grep -n '#[0-9A-Fa-f]\{6\}' src/app/\[locale\]/\(dashboard\)/page.tsx` -- should return zero matches.

Run: `grep -c 'style=' src/app/\[locale\]/\(dashboard\)/page.tsx` -- should return 0 (or only non-color dynamic styles).

Run: `pnpm tsc --noEmit` -- passes with no errors.
  </verify>
  <done>
Dashboard home page has zero hardcoded hex values in inline styles. All color styling uses semantic Tailwind classes (bg-black, text-cream, text-neutral-400, border-cream, etc.). Conditional styling uses cn() utility with Tailwind classes. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "style=" src/app/\[locale\]/\(dashboard\)/page.tsx src/app/\[locale\]/\(dashboard\)/meal-plan/page.tsx src/app/\[locale\]/\(dashboard\)/workout-plan/page.tsx` -- zero color-related inline styles
2. `grep -rn "onMouseEnter\|onMouseLeave" src/app/\[locale\]/\(dashboard\)/` -- zero hover event handlers
3. `grep -rn "useState.*hover\|useState.*Hover" src/app/\[locale\]/\(dashboard\)/` -- zero hover state variables
4. `pnpm tsc --noEmit` -- clean TypeScript compilation
5. `pnpm build` -- successful production build
</verification>

<success_criteria>
- Zero inline style={{}} attributes with hardcoded hex color values in the three dashboard page files
- Zero useState hover anti-patterns in any dashboard page
- All hover effects use Tailwind hover: pseudo-class
- TypeScript compiles with no errors
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-rebrand-visual-audit/02-01-SUMMARY.md`
</output>
