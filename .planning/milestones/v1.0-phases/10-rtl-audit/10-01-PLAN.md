---
phase: 10-rtl-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/pagination.tsx
  - src/components/ui/select.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/toast.tsx
  - src/components/ui/brutalist-multi-select.tsx
autonomous: true

must_haves:
  truths:
    - "All shadcn/ui components use logical CSS properties (ms-/me-/ps-/pe-/start-/end-/border-s-/border-e-/text-start/text-end) instead of physical directional properties"
    - "Components render correctly in both LTR and RTL without manual class toggling"
  artifacts:
    - path: "components.json"
      provides: "RTL configuration for future shadcn component additions"
      contains: "rtl"
    - path: "src/components/ui/tabs.tsx"
      provides: "RTL-aware tab borders"
      contains: "border-e-4"
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "RTL-aware dropdown positioning and icons"
      contains: "ms-auto"
    - path: "src/components/ui/dialog.tsx"
      provides: "RTL-aware dialog close button positioning"
      contains: "end-4"
    - path: "src/components/ui/select.tsx"
      provides: "RTL-aware select item checkmark positioning"
      contains: "start-3"
  key_links:
    - from: "components.json"
      to: "src/components/ui/*"
      via: "shadcn CLI config"
      pattern: "rtl.*true"
---

<objective>
Convert all shadcn/ui components from physical CSS properties to logical properties for RTL support.

Purpose: shadcn/ui components use physical directional classes (ml-*, right-*, border-r-*, text-left) that don't flip in RTL mode. Converting to logical properties (ms-*, end-*, border-e-*, text-start) makes all UI primitives automatically RTL-aware.

Output: All 8 shadcn/ui component files converted to logical properties, components.json updated with RTL config.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rtl-audit/10-RESEARCH.md
@components.json
@src/components/ui/tabs.tsx
@src/components/ui/dialog.tsx
@src/components/ui/dropdown-menu.tsx
@src/components/ui/select.tsx
@src/components/ui/pagination.tsx
@src/components/ui/toast.tsx
@src/components/ui/button.tsx
@src/components/ui/brutalist-multi-select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn RTL migration CLI and update components.json</name>
  <files>
    components.json
    src/components/ui/button.tsx
    src/components/ui/dialog.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/pagination.tsx
    src/components/ui/select.tsx
    src/components/ui/tabs.tsx
    src/components/ui/toast.tsx
    src/components/ui/brutalist-multi-select.tsx
  </files>
  <action>
    First, try running the shadcn RTL migration CLI:
    ```
    pnpm dlx shadcn@latest migrate rtl src/components/ui
    ```

    If the CLI works, verify the conversions. If it fails or doesn't exist yet, do manual conversion.

    **Manual conversion rules (apply to all 8 files):**

    Physical to Logical property mapping:
    - `ml-*` to `ms-*` (margin-left to margin-inline-start)
    - `mr-*` to `me-*` (margin-right to margin-inline-end)
    - `pl-*` to `ps-*` (padding-left to padding-inline-start)
    - `pr-*` to `pe-*` (padding-right to padding-inline-end)
    - `left-*` to `start-*` (inset-left to inset-inline-start)
    - `right-*` to `end-*` (inset-right to inset-inline-end)
    - `border-l-*` to `border-s-*` (border-left to border-inline-start)
    - `border-r-*` to `border-e-*` (border-right to border-inline-end)
    - `text-left` to `text-start`
    - `text-right` to `text-end`

    **Specific file changes (from grep results):**

    `tabs.tsx`:
    - `border-r-4` to `border-e-4`
    - `last:border-r-0` to `last:border-e-0`

    `dialog.tsx`:
    - `right-4 top-4` to `end-4 top-4`
    - `sm:text-left` to `sm:text-start`

    `dropdown-menu.tsx`:
    - `pl-8` to `ps-8` (3 instances)
    - `ml-auto` to `ms-auto` (2 instances)
    - `pl-10` to `ps-10` (2 instances)
    - `pr-4` to `pe-4` (2 instances)
    - `left-3` to `start-3` (2 instances)
    - `data-[side=left]:slide-in-from-right-2` and `data-[side=right]:slide-in-from-left-2` - keep as-is (these are animation origin directions from Radix, not layout properties)

    `select.tsx`:
    - `pl-10` to `ps-10`
    - `pr-4` to `pe-4`
    - `left-3` to `start-3`
    - Same animation classes as dropdown-menu: keep as-is

    `pagination.tsx`:
    - `pl-2.5` to `ps-2.5`
    - `pr-2.5` to `pe-2.5`

    `toast.tsx`:
    - `sm:right-0` to `sm:end-0`
    - `right-2 top-2` to `end-2 top-2`

    `button.tsx`:
    - `-ml-1 mr-2` to `-ms-1 me-2` (spinner icon)

    `brutalist-multi-select.tsx`:
    - `text-left` to `text-start`
    - `-ml-0.5` to `-ms-0.5`
    - `first:ml-0` to `first:ms-0`

    **Update components.json:** Add `"rtl": true` at the top level so future shadcn component additions use logical properties by default.

    **IMPORTANT:** Do NOT change `slide-in-from-left-*` / `slide-in-from-right-*` animation classes. These are directional animation origins from Radix UI and are separate from layout.
  </action>
  <verify>
    Run `pnpm tsc --noEmit` to confirm no type errors.
    Grep for remaining physical properties: `grep -rn "ml-\|mr-\|pl-\|pr-\|left-\|right-\|border-l-\|border-r-\|text-left\|text-right" src/components/ui/` should only show animation-related classes (slide-in-from-*) and `inset-y-0` (vertical, not directional).
  </verify>
  <done>
    All 8 shadcn/ui component files use logical CSS properties. components.json has RTL config. No physical directional properties remain in UI components (except animation origins).
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- `grep -rn "\\bml-\\|\\bmr-\\|\\bpl-\\|\\bpr-\\|\\bleft-\\|\\bright-\\|\\bborder-l-\\|\\bborder-r-\\|\\btext-left\\|\\btext-right" src/components/ui/` shows only animation-related matches
- `components.json` contains `"rtl": true`
</verification>

<success_criteria>
All shadcn/ui primitives (button, dialog, dropdown-menu, pagination, select, tabs, toast, brutalist-multi-select) use logical CSS properties that automatically respect RTL direction. Future shadcn component additions will also use logical properties via components.json config.
</success_criteria>

<output>
After completion, create `.planning/phases/10-rtl-audit/10-01-SUMMARY.md`
</output>
