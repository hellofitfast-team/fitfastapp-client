---
phase: 13-page-level-renovation
plan: 01
type: execute
wave: 2
depends_on: ["13-06"]
files_modified:
  - apps/client/src/app/[locale]/(dashboard)/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05]

must_haves:
  truths:
    - "Home screen shows a rotating motivational greeting with user's first name that changes every 5 seconds"
    - "On mobile, home screen content is displayed in a horizontal carousel with auto-scroll and dot indicators"
    - "On desktop, home screen content is displayed in a 2-column grid instead of carousel"
    - "Quick stats widgets (weight trend, streak, next check-in) are displayed in rounded cards with bold values and muted labels"
    - "A coach message banner appears when there is an unread ticket reply"
    - "A plan cycle countdown (Day X of 14) is visible with a linear progress bar"
  artifacts:
    - path: "apps/client/src/app/[locale]/(dashboard)/page.tsx"
      provides: "Full home screen renovation with carousel, greeting, stats, banner, countdown"
      min_lines: 200
  key_links:
    - from: "apps/client/src/app/[locale]/(dashboard)/page.tsx"
      to: "@fitfast/ui/widget-card"
      via: "import WidgetCard"
      pattern: "import.*WidgetCard.*from.*@fitfast/ui"
    - from: "apps/client/src/app/[locale]/(dashboard)/page.tsx"
      to: "next-intl"
      via: "useTranslations for motivational messages"
      pattern: "t\\(.*motivational"
---

<objective>
Renovate the home/dashboard page with rotating motivational greeting, mobile carousel with auto-scroll, quick stats WidgetCards, coach message banner, and plan cycle countdown.

Purpose: The home screen is the first thing users see — it must feel modern, motivating, and information-rich. This transforms the current static grid into a dynamic, carousel-driven mobile experience.
Output: Fully renovated home page with carousel (mobile) / grid (desktop), rotating greeting, and all HOME-* requirements met.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-page-level-renovation/13-CONTEXT.md
@.planning/phases/13-page-level-renovation/13-RESEARCH.md
@.planning/phases/12-design-tokens-primitives/12-03-SUMMARY.md
@apps/client/src/app/[locale]/(dashboard)/page.tsx
@apps/client/src/messages/en.json
@apps/client/src/messages/ar.json
@packages/ui/src/widget-card.tsx
@packages/ui/src/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renovate home page with carousel, greeting, stats, banner, and countdown</name>
  <files>apps/client/src/app/[locale]/(dashboard)/page.tsx</files>
  <action>
Rewrite the dashboard home page. Preserve ALL existing data fetching hooks (useDashboardData, useTickets, etc.) and the "use client" directive. The page structure should be:

**1. Rotating Motivational Greeting (HOME-01):**
- `useState` for `messageIndex`, `useEffect` with `setInterval` rotating every 5 seconds through 7 messages (index 0-6)
- Use `t(`dashboard.motivational.${messageIndex}`, { name: userName })` where userName comes from the existing dashboard data
- Add `key={messageIndex}` on the greeting `<h1>` to retrigger `animate-fade-in` on each change
- Below greeting, show current date formatted with weekday using locale-aware formatting
- Use `text-2xl font-bold` for the greeting, `text-sm text-muted-foreground` for the date

**2. Coach Message Banner (HOME-04):**
- Conditionally render when there is an unread coach response (check tickets data for status === "coach_responded")
- Render as a dismissible banner: `bg-primary/10 border border-primary/20 rounded-xl p-3 flex items-center gap-3`
- Show coach icon (Shield from lucide-react), message text from `t("dashboard.coachMessageBanner")`, and a "View" link to `/tickets/{ticketId}`
- Dismissible via `useState` boolean — dismiss hides banner for current render session (no localStorage persistence needed)

**3. Mobile Carousel (HOME-02) / Desktop Grid (HOME-03, HOME-05):**

**Mobile (lg:hidden):**
- Create a `HomeCarousel` inline component (not extracted to separate file)
- Wrapper: `<div className="relative -mx-4">`
- Scrollable track: `<div ref={scrollRef} className="flex overflow-x-auto snap-x snap-mandatory scrollbar-hide">`
- Each card: `<div className="flex-shrink-0 w-full snap-center px-4">`
- Auto-scroll with `setInterval` every 4 seconds calling `scrollToIndex(next)`. Clean up interval on unmount.
- For scrollToIndex: use `scrollIntoView({ behavior: 'smooth', inline: 'center' })` on card refs (NOT `scrollLeft` — unreliable in RTL per research). Keep an array of refs using `useRef<(HTMLDivElement | null)[]>([])`.
- Dot indicators: `<div className="flex justify-center gap-1.5 mt-3">` with buttons. Active dot: `w-4 bg-primary`, inactive: `w-1.5 bg-neutral-300`. Both `h-1.5 rounded-full transition-all duration-300`.
- Track active index via `onScroll` handler using `Math.round(el.scrollLeft / el.offsetWidth)` — for RTL, use `useLocale()` to detect "ar" and adjust index calculation.

**Desktop (hidden lg:grid lg:grid-cols-2 gap-4):**
- Same cards but in a static 2-column grid, no carousel behavior.

**Carousel/Grid Cards (4 cards):**

Card 1 — **Today's Stats** (HOME-03): Use `<WidgetCard featureColor="primary">` wrapper. Show 2x2 mini-stats grid inside: current weight (with trend arrow), streak count, next check-in date, completed items today. Each stat: `<div className="text-center"><p className="text-lg font-bold">{value}</p><p className="text-[10px] text-muted-foreground">{label}</p></div>`.

Card 2 — **Today's Meals** (HOME-02): Use `<WidgetCard featureColor="nutrition">`. Show meal count for today, total calories, and a preview list of meal names (max 3, truncated with "...").

Card 3 — **Today's Workout** (HOME-02): Use `<WidgetCard featureColor="fitness">`. Show workout type, exercise count, and estimated duration if available.

Card 4 — **Plan Countdown** (HOME-05): Use `<WidgetCard featureColor="routine">`. Compute current day of plan cycle: `Math.floor((Date.now() - new Date(mealPlan.startDate).getTime()) / 86400000) + 1` clamped to 1-14. Show "Day X of 14" text using `t("dashboard.planDay", { current, total: 14 })`. Add a linear progress bar below: `<div className="h-1.5 bg-neutral-100 rounded-full"><div className="h-full bg-routine rounded-full transition-all" style={{ width: `${(current/14)*100}%` }} /></div>`.

**4. Apply stagger animation** to the grid cards using `style={{ animationDelay: \`${i * 50}ms\` }}` and `animate-slide-up` class on each card.

**5. Handle empty state** (HOME-01 / EMPTY-01): If no plan data exists yet, show `<EmptyState variant="primary" icon={Dumbbell} title={t("emptyStates.noPlan.title")} description={t("emptyStates.noPlan.description")} />` — use existing empty state keys or add minimal new ones if needed.

**Important:** Keep existing hook calls, loading state handling, and error handling. Only restructure the JSX rendering.
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — no TypeScript errors. Visually confirm in dev mode at localhost:3000 that the greeting rotates, carousel scrolls on mobile viewport, and desktop shows grid layout.</verify>
  <done>Home page shows rotating motivational greeting, 4-card carousel on mobile with dot indicators and auto-scroll, 2-column grid on desktop, coach message banner when unread reply exists, and plan cycle countdown with progress bar.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @fitfast/client build` passes with zero errors
2. Home page at localhost:3000 shows rotating greeting that changes every 5 seconds
3. On mobile viewport (390px), cards display in horizontal carousel with dot indicators
4. On desktop viewport (1440px), cards display in 2-column grid
5. Coach banner appears conditionally and is dismissible
6. Plan countdown shows correct day and progress bar
7. Arabic locale shows Arabic motivational messages
</verification>

<success_criteria>
- All 5 HOME requirements (HOME-01 through HOME-05) are implemented
- Motivational greeting rotates every 5 seconds with fade animation
- Carousel auto-scrolls and responds to manual swipe on mobile
- Desktop shows static grid layout
- Coach message banner appears when unread coach response exists
- Plan countdown shows Day X of 14 with progress bar
- Build passes without errors (i18n keys provided by Plan 13-06)
</success_criteria>

<output>
After completion, create `.planning/phases/13-page-level-renovation/13-01-SUMMARY.md`
</output>
