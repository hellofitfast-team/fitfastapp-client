---
phase: 13-page-level-renovation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/messages/en.json
  - apps/client/src/messages/ar.json
  - apps/client/src/app/[locale]/(auth)/layout.tsx
  - apps/client/src/app/[locale]/(auth)/login/page.tsx
  - apps/client/src/app/[locale]/(auth)/accept-invite/page.tsx
autonomous: true
requirements: [EMPTY-01, EMPTY-02, EMPTY-03, EMPTY-04, EMPTY-05, EMPTY-06, AUTH-01, AUTH-02]

must_haves:
  truths:
    - "All phase i18n keys (dashboard motivational, ticket status/chat, empty state CTAs) exist in both en.json and ar.json"
    - "All 6 empty states use EmptyState component with correct variant and friendly CTA copy"
    - "Empty state CTA buttons use conversational, encouraging tone per CONTEXT"
    - "Login page has a clean, modern layout with prominent FitFast branding"
    - "Accept-invite (signup) page matches login design"
    - "Auth layout has improved brand section above the form"
  artifacts:
    - path: "apps/client/src/messages/en.json"
      provides: "All phase i18n keys: dashboard motivational, ticket status/chat, empty state actions"
      contains: "motivational"
    - path: "apps/client/src/messages/ar.json"
      provides: "Arabic translations for all phase i18n keys"
      contains: "motivational"
    - path: "apps/client/src/app/[locale]/(auth)/layout.tsx"
      provides: "Improved auth layout with brand section"
    - path: "apps/client/src/app/[locale]/(auth)/login/page.tsx"
      provides: "Polished login page"
  key_links:
    - from: "apps/client/src/messages/en.json"
      to: "emptyStates"
      via: "action keys for CTA buttons"
      pattern: "action.*Let's get started|Time to check in|Your coach is here"
---

<objective>
Add friendly CTA copy to all 6 empty states, and polish the auth pages (login and accept-invite) with improved branding and clean mobile-optimized layout.

Purpose: Empty states are first impressions — friendly CTAs encourage engagement. Auth pages set the tone for the entire app experience.
Output: All 6 empty states have consistent variant/copy, auth pages have modern clean layout.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-page-level-renovation/13-CONTEXT.md
@.planning/phases/13-page-level-renovation/13-RESEARCH.md
@apps/client/src/messages/en.json
@apps/client/src/messages/ar.json
@apps/client/src/app/[locale]/(auth)/layout.tsx
@apps/client/src/app/[locale]/(auth)/login/page.tsx
@apps/client/src/app/[locale]/(auth)/accept-invite/page.tsx
@packages/ui/src/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add empty state CTA keys and verify all 6 empty states</name>
  <files>apps/client/src/messages/en.json, apps/client/src/messages/ar.json</files>
  <action>
**This task consolidates ALL i18n key additions for the entire phase to avoid concurrent write conflicts on en.json/ar.json.**

**Step 1: Add dashboard motivational and home screen keys (for Plan 13-01).**

In the "dashboard" namespace, add:
```json
// en.json
"motivational": {
  "0": "Keep pushing, {name}!",
  "1": "You're on a roll, {name}!",
  "2": "Stay consistent, {name}!",
  "3": "Every rep counts, {name}!",
  "4": "You've got this, {name}!",
  "5": "Strong effort, {name}!",
  "6": "One day at a time, {name}!"
},
"coachMessageBanner": "Your coach responded to your ticket",
"viewTicket": "View",
"planDay": "Day {current} of {total}",
"planProgress": "Plan Progress"
```

```json
// ar.json
"motivational": {
  "0": "!واصل الجهد، {name}",
  "1": "!أنت في المسار الصحيح، {name}",
  "2": "!الاستمرارية هي المفتاح، {name}",
  "3": "!كل تمرين مهم، {name}",
  "4": "!أنت قادر على ذلك، {name}",
  "5": "!مجهود رائع، {name}",
  "6": "!يوم بيوم، {name}"
},
"coachMessageBanner": "ردّ المدرب على تذكرتك",
"viewTicket": "عرض",
"planDay": "اليوم {current} من {total}",
"planProgress": "تقدم الخطة"
```

**Step 2: Add ticket i18n keys (for Plan 13-04).**

In the "tickets" namespace, add:
```json
// en.json
"status": {
  "open": "Open",
  "coachResponded": "Waiting",
  "closed": "Closed"
},
"lastMessage": "Last message",
"chat": {
  "today": "Today",
  "yesterday": "Yesterday"
}
```

```json
// ar.json
"status": {
  "open": "مفتوح",
  "coachResponded": "في الانتظار",
  "closed": "مغلق"
},
"lastMessage": "آخر رسالة",
"chat": {
  "today": "اليوم",
  "yesterday": "أمس"
}
```

**Step 3: Add missing empty state action keys.**

Check each of the following in the "emptyStates" namespace and add "action" keys where missing. CONTEXT specifies these CTA tones:

**en.json — add action keys within emptyStates namespace:**
- `noMealPlan.action`: "Time to check in!" (EMPTY-02 — links to check-in)
- `noWorkoutPlan.action`: "Let's get started!" (EMPTY-03)
- `noCheckIns.action`: should already exist — verify. If missing: "Complete your first check-in!"
- `noTickets.action`: should already exist — verify. If missing: "Your coach is here to help"
- `noTrackingData.action`: "Time to check in!" (EMPTY-06)
- `noPhotos.action`: "Complete a check-in" (for progress photos)

Also add a new empty state for the home page (EMPTY-01):
```json
"noPlan": {
  "title": "Your plan is on the way!",
  "description": "Your coach is preparing your personalized plan. Check back soon!",
  "action": "Let's get started!"
}
```

**ar.json — mirror all new keys in Arabic:**
- `noMealPlan.action`: "!حان وقت التسجيل"
- `noWorkoutPlan.action`: "!هيا نبدأ"
- `noTrackingData.action`: "!حان وقت التسجيل"
- `noPhotos.action`: "أكمل تسجيل الدخول"
- `noPlan.title`: "!خطتك في الطريق"
- `noPlan.description`: "مدربك يحضّر خطتك الشخصية. تفقد قريباً!"
- `noPlan.action`: "!هيا نبدأ"

**Step 2: Audit all pages that show empty states.**

Verify (read the files, do NOT modify in this task) that each page uses EmptyState with the correct variant. Document which pages need updates in their next plan/task:

| Page | Empty State ID | Variant | Icon | Current Status |
|------|---------------|---------|------|----------------|
| Home (page.tsx) | EMPTY-01 | primary | Dumbbell | Needs adding (Plan 01 handles) |
| Meal plan | EMPTY-02 | nutrition | UtensilsCrossed | Needs adding (Plan 02 handles) |
| Workout plan | EMPTY-03 | fitness | Dumbbell | Needs adding (Plan 03 handles) |
| Check-in | EMPTY-04 | primary | ClipboardCheck | Verify existing |
| Tickets | EMPTY-05 | primary | MessageSquare | Already uses EmptyState — verify variant |
| Tracking | EMPTY-06 | routine | Target | Already uses EmptyState — verify variant |

Each plan that renovates a page is responsible for ensuring its empty state uses the correct variant and action key. This task only adds the i18n keys.

**Important:** Do NOT modify any existing keys. Only ADD new keys. If a key already exists with different copy, leave the existing copy (the page plans will reference these keys).
  </action>
  <verify>Validate both JSON files: `node -e "JSON.parse(require('fs').readFileSync('apps/client/src/messages/en.json'))"` and same for ar.json. Run `pnpm --filter @fitfast/client build`.</verify>
  <done>All phase i18n keys added: dashboard.motivational.0-6, dashboard.coachMessageBanner/viewTicket/planDay/planProgress, tickets.status.*, tickets.chat.*, and all 6 empty state action keys in both en.json and ar.json with friendly CTA copy. Home page noPlan keys added.</done>
</task>

<task type="auto">
  <name>Task 2: Polish auth layout and login/accept-invite pages</name>
  <files>apps/client/src/app/[locale]/(auth)/layout.tsx, apps/client/src/app/[locale]/(auth)/login/page.tsx, apps/client/src/app/[locale]/(auth)/accept-invite/page.tsx</files>
  <action>
**Auth Layout (layout.tsx) — AUTH-01, AUTH-02:**

Enhance the auth layout with a more prominent brand section. Research recommends centered card (not split layout) since mobile is primary viewport.

- Keep the existing centered layout structure (`max-w-md mx-auto` or similar)
- Improve the header/brand section above the form card:
  - Add a gradient background behind the top section: `bg-gradient-to-b from-primary/5 to-background`
  - Brand logo area: `flex flex-col items-center gap-2 pt-12 pb-8`
  - App name: `text-2xl font-bold text-primary` — "FitFast"
  - Tagline: `text-sm text-muted-foreground` — use existing i18n key or add one like "Your AI-Powered Fitness Coach"
  - Add a subtle fitness icon above the name: `<Dumbbell className="h-10 w-10 text-primary mb-2" />`
- Main content area (children): `px-4 pb-8`
- Keep existing footer if present

**Login Page (login/page.tsx) — AUTH-01:**
- Polish the form card: `rounded-2xl border border-border bg-card p-6 shadow-sm`
- Form inputs: ensure they use `rounded-xl` border styling
- Submit button: ensure it uses the primary Button component with full width
- Magic link option: keep if present, ensure consistent styling
- Add `animate-fade-in` to the form card for entrance animation

**Accept-Invite Page (accept-invite/page.tsx) — AUTH-02:**
- This is the signup equivalent — users land here via email invitation link
- Apply same card styling as login: `rounded-2xl border border-border bg-card p-6 shadow-sm`
- Ensure the form layout matches login page design
- If there's a welcome/invitation message, style it with `text-center mb-6` block above the form
- Add `animate-fade-in` to the form card

**Both pages should:**
- Look cohesive with matching card radius, spacing, and typography
- Feel mobile-optimized (large touch targets, generous padding)
- Use the auth layout's brand section for visual impact

**Do NOT change any form logic, validation, or submission handlers.** Only CSS/layout changes.
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — no errors. Visually confirm at localhost:3000/en/login that the brand section is prominent and the form card looks modern.</verify>
  <done>Auth layout has gradient brand section with app name and icon. Login and accept-invite pages have matching modern card layouts with rounded-2xl borders and entrance animation. Both pages look cohesive on mobile viewport.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @fitfast/client build` passes
2. Both en.json and ar.json contain action keys for all 6 empty states
3. Home page noPlan keys exist in both languages
4. Auth layout shows gradient background with FitFast branding
5. Login page form card has rounded-2xl border and shadow
6. Accept-invite page matches login design
7. Both auth pages have entrance animation
</verification>

<success_criteria>
- EMPTY-01 through EMPTY-06: All action keys present in both language files with friendly CTA copy
- AUTH-01: Login page has modern clean layout with prominent branding
- AUTH-02: Accept-invite page matches login design
- CTA copy uses conversational, encouraging tone per CONTEXT
- Auth layout has improved brand section (gradient, icon, app name)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-page-level-renovation/13-06-SUMMARY.md`
</output>
