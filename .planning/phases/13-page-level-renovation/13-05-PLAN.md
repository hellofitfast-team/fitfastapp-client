---
phase: 13-page-level-renovation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/tracking/_components/tracking-header.tsx
  - apps/client/src/app/[locale]/(dashboard)/tracking/_components/meal-tracking.tsx
  - apps/client/src/app/[locale]/(dashboard)/tracking/_components/workout-tracking.tsx
  - apps/client/src/app/[locale]/(dashboard)/tracking/_components/daily-reflection.tsx
  - apps/client/src/app/[locale]/(dashboard)/progress/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/progress/_components/stats-overview.tsx
autonomous: true
requirements: [TRACK-01, TRACK-02]

must_haves:
  truths:
    - "Tracking page sub-components (header, meal tracking, workout tracking, reflection) use modern card layout with rounded borders and semantic color accents"
    - "Progress page stats cards are displayed in rounded cards with bold values and muted labels, using WidgetCard"
    - "Progress chart wrappers have clean border and shadow treatment"
  artifacts:
    - path: "apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx"
      provides: "Renovated tracking orchestrator page"
    - path: "apps/client/src/app/[locale]/(dashboard)/progress/page.tsx"
      provides: "Renovated progress page with WidgetCard stats"
    - path: "apps/client/src/app/[locale]/(dashboard)/progress/_components/stats-overview.tsx"
      provides: "Stats overview with WidgetCard wrappers"
  key_links:
    - from: "apps/client/src/app/[locale]/(dashboard)/progress/_components/stats-overview.tsx"
      to: "@fitfast/ui/widget-card"
      via: "import for stat cards"
      pattern: "import.*WidgetCard"
---

<objective>
Renovate tracking and progress pages with modern card layouts, WidgetCard primitives, and consistent visual polish.

Purpose: These pages complete the core dashboard experience. Consistent styling with the home/plan pages creates a cohesive, professional feel.
Output: Tracking and progress pages renovated with consistent design system application.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-page-level-renovation/13-CONTEXT.md
@.planning/phases/13-page-level-renovation/13-RESEARCH.md
@apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx
@apps/client/src/app/[locale]/(dashboard)/tracking/_components/tracking-header.tsx
@apps/client/src/app/[locale]/(dashboard)/tracking/_components/meal-tracking.tsx
@apps/client/src/app/[locale]/(dashboard)/tracking/_components/workout-tracking.tsx
@apps/client/src/app/[locale]/(dashboard)/tracking/_components/daily-reflection.tsx
@apps/client/src/app/[locale]/(dashboard)/progress/page.tsx
@apps/client/src/app/[locale]/(dashboard)/progress/_components/stats-overview.tsx
@packages/ui/src/widget-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renovate tracking page and its sub-components</name>
  <files>apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx, apps/client/src/app/[locale]/(dashboard)/tracking/_components/tracking-header.tsx, apps/client/src/app/[locale]/(dashboard)/tracking/_components/meal-tracking.tsx, apps/client/src/app/[locale]/(dashboard)/tracking/_components/workout-tracking.tsx, apps/client/src/app/[locale]/(dashboard)/tracking/_components/daily-reflection.tsx</files>
  <action>
Renovate each tracking sub-component with modern card styling. Preserve all existing data props, logic, and hook usage.

**tracking-header.tsx:**
- Apply PageHeader-style layout: large title, subtitle with today's date, compact summary stats row
- Summary stats row: use `flex gap-3` with pill badges showing completion percentages for meals/workout tracking

**meal-tracking.tsx:**
- Wrap meal tracking items in `rounded-xl border border-border bg-card` cards
- Each tracked meal: show meal name, checkbox/completion indicator, calorie display
- Add `animate-slide-up` with stagger delay to each item
- Use `bg-nutrition/10 text-nutrition` for nutrition-related accents

**workout-tracking.tsx:**
- Same card treatment as meal tracking
- Exercise items with completion checkboxes
- Use `bg-fitness/10 text-fitness` for fitness accents
- Add stagger animation

**daily-reflection.tsx:**
- Wrap in a `rounded-xl border border-border bg-card p-4` card
- Style the textarea/input for reflection notes with `rounded-xl border border-border bg-background`
- Add mood/energy selectors if they exist — apply pill button styling
- Section title with icon: `<div className="flex items-center gap-2 mb-3"><Heart className="h-4 w-4 text-primary" /><h3 className="font-semibold text-sm">...</h3></div>`

**tracking/page.tsx (orchestrator):**
- Add `space-y-4` between sub-components
- Ensure the PageHeader is used at top (import from @fitfast/ui/page-header if available, or use semantic heading)
- The existing EmptyState for no-plan state should use `variant="routine"` and friendly CTA copy

**TRACK-01 requirement:** Ensure the overall tracking page uses modern card layout — this means each sub-component renders inside its own card container, not using global dividers.
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — no errors. Visually confirm tracking page at localhost:3000/en/tracking shows card-based layout with consistent styling.</verify>
  <done>Tracking page and all 4 sub-components use modern card layout with semantic color accents, stagger animations, and consistent border/shadow treatment.</done>
</task>

<task type="auto">
  <name>Task 2: Renovate progress page stats and chart wrappers</name>
  <files>apps/client/src/app/[locale]/(dashboard)/progress/page.tsx, apps/client/src/app/[locale]/(dashboard)/progress/_components/stats-overview.tsx</files>
  <action>
**Progress Page (TRACK-02):**

**stats-overview.tsx:**
- Wrap each stat card in `<WidgetCard>` (import from @fitfast/ui/widget-card)
- Stats grid: `grid grid-cols-2 gap-3` (already similar — verify and polish)
- Each stat: large bold value, small muted label, optional trend indicator (up/down arrow icon with green/red color)
- Add `animate-slide-up` with stagger delay

**progress/page.tsx:**
- Tab navigation: keep existing Tabs component but ensure tab triggers use `rounded-xl` pill style with active state using `bg-primary text-primary-foreground`
- Chart wrapper card: `rounded-xl border border-border bg-card p-4 shadow-sm`
- History tab items: `rounded-xl border border-border bg-card p-3 mb-2` for each history entry with stagger animation
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — no errors. Verify progress stats use WidgetCard, chart has rounded card wrapper.</verify>
  <done>Progress page uses WidgetCard for stats and clean chart wrappers. History items have card treatment with stagger animation.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @fitfast/client build` passes
2. Tracking page: all sub-components render in individual card containers
3. Progress page: stats use WidgetCard, chart has rounded card wrapper, history items have cards
4. All pages maintain their existing functionality (data fetching, forms, submission)
</verification>

<success_criteria>
- TRACK-01: Tracking page uses card-based layout for all sub-components
- TRACK-02: Progress page stats use WidgetCard, chart has clean card wrapper
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-page-level-renovation/13-05-SUMMARY.md`
</output>
