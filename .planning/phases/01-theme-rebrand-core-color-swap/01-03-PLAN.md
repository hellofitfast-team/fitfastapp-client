---
phase: 01-theme-rebrand-core-color-swap
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/[locale]/(dashboard)/page.tsx
  - src/app/[locale]/(dashboard)/loading.tsx
  - src/app/[locale]/(dashboard)/meal-plan/page.tsx
  - src/app/[locale]/(dashboard)/meal-plan/loading.tsx
  - src/app/[locale]/(dashboard)/workout-plan/page.tsx
  - src/app/[locale]/(dashboard)/workout-plan/loading.tsx
  - src/app/[locale]/(dashboard)/check-in/page.tsx
  - src/app/[locale]/(dashboard)/tracking/page.tsx
  - src/app/[locale]/(dashboard)/progress/page.tsx
  - src/app/[locale]/(dashboard)/progress/loading.tsx
  - src/app/[locale]/(dashboard)/settings/page.tsx
  - src/app/[locale]/(dashboard)/tickets/page.tsx
  - src/app/[locale]/(dashboard)/faq/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard home page uses Royal Blue for primary actions and accents"
    - "Meal plan and workout plan pages use Royal Blue for section headers and CTAs"
    - "Check-in page uses Royal Blue for active step indicators and brand accents"
    - "Tracking page uses Royal Blue for section headers and completion states"
    - "Progress page uses Royal Blue for stat cards and section headers"
    - "Settings, tickets, and FAQ pages use Royal Blue for primary accents"
    - "No hardcoded #FF3B00 or #00FF94 hex values remain in any dashboard file"
    - "Loading skeleton states use Royal Blue tints instead of orange/green"
    - "Weight gain/loss semantic colors preserved (gain=error, loss=success)"
  artifacts:
    - path: "src/app/[locale]/(dashboard)/page.tsx"
      provides: "Dashboard home with Royal Blue theme"
    - path: "src/app/[locale]/(dashboard)/check-in/page.tsx"
      provides: "Check-in wizard with Royal Blue step indicators"
    - path: "src/app/[locale]/(dashboard)/progress/page.tsx"
      provides: "Progress page with Royal Blue stat cards"
    - path: "src/app/[locale]/(dashboard)/meal-plan/page.tsx"
      provides: "Meal plan page with Royal Blue accents"
    - path: "src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      provides: "Workout plan page with Royal Blue accents"
  key_links:
    - from: "src/app/globals.css"
      to: "All dashboard page files"
      via: "Tailwind semantic classes (bg-primary, text-primary, etc.)"
      pattern: "bg-primary|text-primary|border-primary"
---

<objective>
Replace all hardcoded orange (#FF3B00) and green (#00FF94) hex values across all dashboard page files with semantic Tailwind classes.

Purpose: Dashboard pages are where users spend most of their time. This plan covers the 13 dashboard files (pages + loading states) to complete the Royal Blue color swap across the entire client-facing UI.

Output: 13 dashboard files updated with zero hardcoded brand color hex values. The entire dashboard uses semantic Tailwind classes resolving to Royal Blue.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-rebrand-core-color-swap/01-RESEARCH.md
@.planning/phases/01-theme-rebrand-core-color-swap/01-01-SUMMARY.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex in dashboard home, meal plan, and workout plan pages</name>
  <files>
    src/app/[locale]/(dashboard)/page.tsx
    src/app/[locale]/(dashboard)/loading.tsx
    src/app/[locale]/(dashboard)/meal-plan/page.tsx
    src/app/[locale]/(dashboard)/meal-plan/loading.tsx
    src/app/[locale]/(dashboard)/workout-plan/page.tsx
    src/app/[locale]/(dashboard)/workout-plan/loading.tsx
  </files>
  <action>
    Read each file and replace hardcoded hex values with semantic Tailwind classes.

    **Color mapping rules (same as Plan 02):**
    - `#FF3B00` as brand/primary -> `primary`
    - `#FF3B00` as error -> `error-500`
    - `#00FF94` as brand/accent -> `primary` (becomes Royal Blue)
    - `#00FF94` as success/completion -> `success-500`
    - `#FFFEF5` -> `cream`
    - Keep `#000000` references as-is (brutalist borders/shadows)

    **dashboard/page.tsx (13 #FF3B00, some #00FF94):**
    - StatCard hover: `hover:bg-[#FF3B00]` -> `hover:bg-primary` (the isOrange conditional)
    - The `isOrange` logic that alternates orange/non-orange cards: replace with a simpler approach since both colors become primary. Change `isOrange ? "hover:bg-[#FF3B00]" : "hover:bg-black"` to keep the alternating pattern but use `hover:bg-primary` for accent cards.
    - `group-hover:text-[#00FF94]` -> `group-hover:text-primary`
    - Error text `text-[#FF3B00]` for "ERROR LOADING DASHBOARD" -> `text-error-500`
    - Username highlight `text-[#FF3B00]` -> `text-primary`
    - Meal done states: `bg-black text-[#00FF94]` -> `bg-black text-primary` (completion indicator on dashboard = brand accent)
    - Meal hover: `hover:bg-[#FF3B00]` -> `hover:bg-primary`
    - Meal checkbox completed: `bg-[#00FF94]` -> `bg-primary`
    - Progress link: `hover:bg-[#FF3B00]` -> `hover:bg-primary`
    - `text-[#FFFEF5]` -> `text-cream`

    **dashboard/loading.tsx (1 occurrence):**
    - Skeleton: `bg-[#FF3B00]/20` -> `bg-primary/20`

    **meal-plan/page.tsx (19 occurrences):**
    - Empty state icon: `bg-[#FF3B00]` -> `bg-primary`
    - Generating state icon: `bg-[#FF3B00]` -> `bg-primary`
    - Generate button: `hover:bg-[#FF3B00]` -> `hover:bg-primary`, `text-[#FFFEF5]` -> `text-cream`
    - Failed state icon: `bg-[#FF3B00]` -> `bg-error-500` (failure = error context)
    - Failed state section: `bg-[#FF3B00]` -> `bg-error-500`
    - Active plan header (green sections): `bg-[#00FF94]` for meal section headers -> `bg-primary` (these are informational headers, not success indicators)
    - Swap alternatives: `bg-[#00FF94]/10` -> `bg-primary/10`
    - Coach notes label: `text-[#00FF94]` -> `text-primary`
    - Loading skeleton: `bg-[#00FF94]/30` and `bg-[#FF3B00]/30` -> `bg-primary/30`

    **meal-plan/loading.tsx (1 occurrence):**
    - `bg-[#00FF94]/30` -> `bg-primary/30`
    - `bg-[#FF3B00]/30` if present -> `bg-primary/30`

    **workout-plan/page.tsx (20 occurrences):**
    - Empty state icon: `bg-[#00FF94]` -> `bg-primary`
    - Generating state icon: `bg-[#00FF94]` -> `bg-primary`
    - Failed state: `bg-[#FF3B00]` -> `bg-error-500`
    - Day headers: `bg-[#FF3B00]` -> `bg-primary` (workout day section headers)
    - Exercise headers: `bg-[#00FF94]` -> `bg-primary`
    - Exercise icon: `bg-[#FF3B00]` -> `bg-primary`
    - Warmup label: `text-[#00FF94]` -> `text-primary`, `bg-[#00FF94]` -> `bg-primary`
    - Set info: `bg-[#FF3B00]/10 text-[#FF3B00]` -> `bg-primary/10 text-primary`
    - Alternative info: `bg-[#00FF94]/10` -> `bg-primary/10`
    - Complete button: `hover:bg-[#00FF94]` -> `hover:bg-primary`
    - Failed section: `bg-[#FF3B00]` -> `bg-error-500`
    - Active plan section: `bg-[#00FF94]` -> `bg-primary` (when plan exists and is active)

    **workout-plan/loading.tsx (1 occurrence):**
    - `bg-[#FF3B00]/30` -> `bg-primary/30`
  </action>
  <verify>
    1. Run `grep -rn '#FF3B00\|#00FF94' src/app/\[locale\]/\(dashboard\)/page.tsx src/app/\[locale\]/\(dashboard\)/loading.tsx src/app/\[locale\]/\(dashboard\)/meal-plan/ src/app/\[locale\]/\(dashboard\)/workout-plan/` - should return ZERO results
    2. Run `pnpm tsc --noEmit` to verify no TypeScript errors
  </verify>
  <done>
    - Zero hardcoded #FF3B00 or #00FF94 in dashboard home, meal plan, and workout plan files
    - Failed/error states use error-500
    - Active/brand states use primary
    - Loading skeletons use primary tints
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex in check-in, tracking, progress, settings, tickets, and FAQ pages</name>
  <files>
    src/app/[locale]/(dashboard)/check-in/page.tsx
    src/app/[locale]/(dashboard)/tracking/page.tsx
    src/app/[locale]/(dashboard)/progress/page.tsx
    src/app/[locale]/(dashboard)/progress/loading.tsx
    src/app/[locale]/(dashboard)/settings/page.tsx
    src/app/[locale]/(dashboard)/tickets/page.tsx
    src/app/[locale]/(dashboard)/faq/page.tsx
  </files>
  <action>
    Read each file and replace hardcoded hex values with semantic Tailwind classes.

    **check-in/page.tsx (27 occurrences - largest file):**
    - Step progress: completed steps `bg-[#00FF94]` -> `bg-success-500` (completed = success), current step `bg-[#FF3B00]` -> `bg-primary` (active = brand)
    - Step number text: completed `text-[#00FF94]` -> `text-success-500`, current `text-[#FF3B00]` -> `text-primary`
    - Section headers (weight, workout, dietary, photos): `bg-[#FF3B00]` -> `bg-primary` (workout section), `bg-[#00FF94]` -> `bg-primary` (other sections). Since all are informational section headers, use `bg-primary` for all.
    - Section icons: `text-[#FF3B00]` -> `text-primary`, `text-[#00FF94]` -> `text-primary` (icons inside black icon containers, the text color shows on the black bg)
    - Wait - icons inside `bg-black` containers with `text-[#FF3B00]` or `text-[#00FF94]` -> both become `text-primary`
    - Rating display: `text-[#FF3B00]` for metric value -> `text-primary`
    - Rating buttons active: `bg-[#FF3B00] text-white` -> `bg-primary text-white` (workout performance)
    - Rating buttons active: `bg-[#00FF94] text-black` -> `bg-primary text-cream` (dietary adherence) - note: text needs to be cream/white on blue background for contrast
    - Dietary adherence value: `text-[#FF3B00]` -> `text-primary`
    - Photo delete button: `bg-[#FF3B00]` -> `bg-error-500` (destructive action)
    - Review section icon: `bg-[#00FF94]` -> `bg-primary`
    - Navigation buttons: `hover:bg-[#FF3B00]` -> `hover:bg-primary`, `text-[#FFFEF5]` -> `text-cream`
    - Submit button: `bg-[#00FF94]` -> `bg-primary`, `hover:bg-[#FF3B00] hover:text-white` -> `hover:bg-primary-dark hover:text-white`
    - Error messages: `text-[#FF3B00]` for validation -> `text-error-500`

    **tracking/page.tsx (14 occurrences):**
    - Loading spinner: `text-[#FF3B00]` -> `text-primary`
    - Empty state icon: `bg-[#00FF94]` -> `bg-primary`
    - Tagline: `text-[#00FF94]` -> `text-primary`
    - Stats header: `bg-[#FF3B00]` -> `bg-primary`
    - Meal header: `bg-[#FF3B00]` -> `bg-primary`
    - Meal icon: `text-[#FF3B00]` -> `text-primary`
    - Completion checkmarks: `bg-[#00FF94]` -> `bg-success-500` (completed items)
    - `bg-[#FFFEF5]` for uncompleted -> `bg-cream`
    - Exercise stats: `bg-[#00FF94]` -> `bg-primary`, `text-[#00FF94]` -> `text-primary`
    - Exercise completion indicator: `bg-[#00FF94]` -> `bg-success-500`
    - Workout completed toggle: `bg-[#00FF94]` for completed -> `bg-success-500`
    - Coach notes header: `bg-[#00FF94]` -> `bg-primary`
    - Coach notes icon: `text-[#00FF94]` -> `text-primary`
    - Save button: `hover:bg-[#FF3B00]` -> `hover:bg-primary`, `text-[#FFFEF5]` -> `text-cream`

    **progress/page.tsx (17 occurrences):**
    - Loading state skeleton: `bg-[#FF3B00]` -> `bg-primary`
    - Loading spinner: `text-[#FF3B00]` -> `text-primary`
    - Empty state icon: `bg-[#00FF94]` -> `bg-primary`
    - Tagline: `text-[#00FF94]` -> `text-primary`
    - Date range active: `bg-[#00FF94]` -> `bg-primary`
    - Weight change indicators: This is SEMANTIC - weight loss `text-[#00FF94]` -> `text-success-500` (positive outcome), weight gain `text-[#FF3B00]` -> `text-error-500` (concerning outcome). Keep these as semantic colors.
    - Trend icons: `text-[#00FF94]` for down trend -> `text-success-500`, `text-[#FF3B00]` for up trend -> `text-error-500`
    - Stat card: `bg-[#FF3B00]` -> `bg-primary`
    - Photos section: `bg-[#00FF94]` for header -> `bg-primary`
    - Photo icon: `text-[#00FF94]` -> `text-primary`
    - Check-in history icon: `bg-[#FF3B00]` -> `bg-primary`
    - Weight badge: `border-[#FF3B00] bg-[#FF3B00]/10 text-[#FF3B00]` -> `border-primary bg-primary/10 text-primary`
    - Weight unit: `text-[#FF3B00]` -> `text-primary`
    - Check-in history label: `text-[#00FF94]` -> `text-primary`
    - Photo close button: `bg-[#FF3B00]` -> `bg-error-500` (close/dismiss = destructive)
    - `bg-[#FFFEF5]` -> `bg-cream`

    **progress/loading.tsx (3 occurrences):**
    - Stat card skeleton: `bg-[#FF3B00]` -> `bg-primary`
    - Section header skeleton: `bg-[#FF3B00]` -> `bg-primary`
    - Icon skeleton: `bg-[#00FF94]/30` -> `bg-primary/30`

    **settings/page.tsx (11 occurrences):**
    - Section header: `bg-[#FF3B00]` -> `bg-primary`
    - Save button: `hover:bg-[#FF3B00]` -> `hover:bg-primary`
    - Notification icon: `bg-[#00FF94]` -> `bg-primary`
    - Toggle on state: `bg-[#00FF94]` -> `bg-primary`
    - Tagline: `text-[#00FF94]` -> `text-primary`
    - Danger zone icon: `bg-[#FF3B00]` -> `bg-error-500` (danger = error context)
    - Logout button: `border-[#FF3B00] bg-[#FF3B00]/10 text-[#FF3B00] hover:bg-[#FF3B00] hover:text-white` -> `border-error-500 bg-error-500/10 text-error-500 hover:bg-error-500 hover:text-white` (destructive action)
    - Subscription section: `bg-[#00FF94]` header -> `bg-primary`
    - Subscription icon: `text-[#00FF94]` -> `text-primary`
    - Subscription badge: `border-[#00FF94] bg-[#00FF94]/10` -> `border-primary bg-primary/10`
    - Progress bar: `bg-[#00FF94]` -> `bg-primary`

    **tickets/page.tsx (12 occurrences):**
    - Priority "high" badge: `border-[#FF3B00] bg-[#FF3B00]/10 text-[#FF3B00]` -> `border-error-500 bg-error-500/10 text-error-500` (high priority = warning/error semantic)
    - Priority "resolved" badge: `border-[#00FF94] bg-[#00FF94]/10 text-black` -> `border-success-500 bg-success-500/10 text-black` (resolved = success semantic)
    - Empty state icon: `bg-[#FF3B00]` -> `bg-primary`
    - Submit button: `hover:bg-[#FF3B00]` -> `hover:bg-primary`, `text-[#FFFEF5]` -> `text-cream`
    - No tickets message: `bg-[#FF3B00]/10` -> `bg-primary/10`
    - Status badges: open `bg-[#FF3B00]` -> `bg-primary`, resolved `bg-[#00FF94]` -> `bg-success-500`
    - Success banner: `bg-[#00FF94]` -> `bg-success-500`
    - New ticket header: `bg-[#00FF94]` -> `bg-primary`

    **faq/page.tsx (6 occurrences):**
    - Icon container: `bg-[#FF3B00]` -> `bg-primary`
    - FAQ icon: `bg-[#00FF94]` -> `bg-primary`
    - Tagline: `text-[#00FF94]` -> `text-primary`
    - Active FAQ item: `bg-[#FF3B00]` -> `bg-primary`
    - Inactive FAQ item: `bg-[#FFFEF5]` -> `bg-cream`
    - CTA section: `bg-[#FF3B00]` -> `bg-primary`
    - Message icon: `text-[#00FF94]` -> `text-primary`
  </action>
  <verify>
    1. Run `grep -rn '#FF3B00\|#00FF94' src/app/\[locale\]/\(dashboard\)/check-in/ src/app/\[locale\]/\(dashboard\)/tracking/ src/app/\[locale\]/\(dashboard\)/progress/ src/app/\[locale\]/\(dashboard\)/settings/ src/app/\[locale\]/\(dashboard\)/tickets/ src/app/\[locale\]/\(dashboard\)/faq/` - should return ZERO results
    2. Run `pnpm tsc --noEmit` to verify no TypeScript errors
    3. Run `pnpm build` to verify full build passes
  </verify>
  <done>
    - Zero hardcoded #FF3B00 or #00FF94 in all 7 dashboard section files
    - Weight change semantic colors preserved (loss=success, gain=error)
    - Ticket priority/status semantic colors preserved (high=error, resolved=success)
    - Danger/destructive actions use error-500
    - All brand/interactive states use primary
    - Full build passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Final verification sweep across entire codebase</name>
  <files></files>
  <action>
    Run a comprehensive grep across the entire src/ directory to find ANY remaining hardcoded #FF3B00 or #00FF94 values.

    1. Run: `grep -rn '#FF3B00' src/` - must return ZERO results
    2. Run: `grep -rn '#00FF94' src/` - must return ZERO results
    3. Run: `grep -rn 'bg-\[#FF3B00\]\|text-\[#FF3B00\]\|border-\[#FF3B00\]' src/` - must return ZERO
    4. Run: `grep -rn 'bg-\[#00FF94\]\|text-\[#00FF94\]\|border-\[#00FF94\]' src/` - must return ZERO
    5. Verify globals.css still has #FF3B00 and #00FF94 ONLY in the `--color-error-500` and `--color-success-500` variable definitions (these are the semantic color definitions, not hardcoded usage)
    6. Run `pnpm build` for final build verification
    7. Verify manifest.json has `"theme_color": "#4169E1"`
    8. Verify layout.tsx has `themeColor: "#4169E1"`

    If any remaining hardcoded values are found in files not covered by Plans 01-03, fix them immediately using the same mapping rules.

    Also check for any `#00cc78` or other green hex variants that may have been used as hover states for the green color.
  </action>
  <verify>
    1. `grep -rn '#FF3B00' src/` returns matches ONLY in globals.css for --color-error-500 and --color-error-600 definitions
    2. `grep -rn '#00FF94' src/` returns matches ONLY in globals.css for --color-success-500 definition
    3. `grep -rn '#CC2F00' src/` returns matches ONLY in globals.css for --color-error-600 definition
    4. `grep -rn '#00CC76' src/` returns matches ONLY in globals.css for --color-success-600 definition
    5. `pnpm build` succeeds
  </verify>
  <done>
    - ZERO hardcoded #FF3B00 or #00FF94 anywhere in src/ except globals.css semantic variable definitions
    - Full build passes
    - Phase 1 success criterion #4 verified: "No hardcoded orange/green color values remain in codebase"
  </done>
</task>

</tasks>

<verification>
1. `grep -rn '#FF3B00\|#00FF94' src/ --include='*.tsx' --include='*.ts'` returns ZERO results (only .css allowed)
2. `grep -c '#FF3B00' src/app/globals.css` returns exactly 2 (error-500 and error-600 definitions only)
3. `grep -c '#00FF94' src/app/globals.css` returns exactly 1 (success-500 definition only)
4. `pnpm build` succeeds without errors
5. All four phase success criteria verified:
   a. CSS variables use Royal Blue
   b. WCAG contrast ratios addressed (Royal Blue #4169E1 has ~4.85:1 against cream)
   c. PWA manifest and meta tags updated
   d. No hardcoded orange/green values remain
</verification>

<success_criteria>
- Every dashboard page uses Royal Blue for primary actions and accents
- Semantic color contexts preserved: weight change (success/error), ticket status (success/error), form validation (error), danger actions (error)
- Zero hardcoded orange/green hex values across the entire codebase (except CSS variable definitions)
- Full application builds successfully
- Phase 1 is complete: Royal Blue is the primary color everywhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-theme-rebrand-core-color-swap/01-03-SUMMARY.md`
</output>
